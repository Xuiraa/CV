/********************
* Background - Card *
********************/

/*** Reglage Card ***/
.js-filter > div[data-tag] > * {
  display: flex;
  justify-content: center;
  align-items: center;

  box-sizing: border-box;
  aspect-ratio: 245 / 167;
  position: relative;
}

.js-filter a {
  position: relative; 
  width: 100%; 
  height: 100%; 
  overflow: hidden;
}

/*Reglage dimension Image */
.js-filter a img {
  position: relative;
  top: 25%; 
  height: 50%;
  object-fit: contain;
  object-position: 50% 50%;
}

/*Ajustement margin de Yootheme */
.js-filter a picture > img {margin-top: 0!important;}


/** Intergration **/

/* Intergration Bg */
.js-filter > div[data-tag] > * {
  background-image: url(images/commons/style/belles_histoires_bg/folder_belles_histoires_fond_couleur.svg);
  background-repeat: no-repeat;
  background-size: 100% 100%;
  background-position: center;
}

/* Integration ruban Web */
.js-filter > div[data-tag*="Web"] a:before {
  position: absolute;
  content: "";
  background-image: url(images/picto/belles_histoires/rubban_web.png);
  background-repeat:  no-repeat;
  background-size: contain;
  
  position: absolute;
  top: -5px;
  left: 35px;

  height: 100px;
  width: 40px;
}
/* Intergration Fond - file*/
.js-filter > div[data-tag] > *:before {
  position: absolute;
  content: "";
  background-image: url(https://digitanie.org/images/commons/style/belles_histoires_bg/FOLDER2.svg);
  background-repeat: no-repeat;
  background-size: 100% 97%;
  background-position: bottom;
  
  height: 100%;
  width: 100%;
}

/*** Animation ***/

/* Ruban */
.js-filter a:hover:before {
  transform: translatey(-25px);
    transition-duration: .5s;
    transition-delay: .5s;
}
/* Files */
/*.js-filter > div[data-tag] > * {
    perspective: 300px;
}
/*.js-filter > div[data-tag] > *:before {
    perspective-origin: left bottom;
    perspective: 200px;
}*/
.js-filter > div[data-tag] > *:hover:before {
    transform-origin: left bottom;
    transform: skew(-10deg) scale(1, 0.85);
    transition-duration: .5s;
    
}
